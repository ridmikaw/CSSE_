// src/config.js

const BASE_URL = 'http://localhost:4000'; // Replace with your production URL if necessary

const API_ENDPOINTS = {
  GET_BINS: `${BASE_URL}/bins`,
  ADD_BIN: `${BASE_URL}/bins`,
};

export default API_ENDPOINTS;
